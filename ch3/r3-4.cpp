#include <iostream>
#include <math.h>
using namespace std;

int main()
{
	// int n = 1; int m = -1;

	// if (n < -m) { cout << n; } else { cout << m; }
	// return 0;
	// int n = 1; 
	// int m = -1;  
	// if (-n >= m) { cout << n; } else { cout << m; }
	// return 0;
	// double x = 0; 
	// double y = 1;  
	// if (abs(x - y) < 1) { cout << x; } else { cout << y; }
	// return 0;
	double x = sqrt(2); 
	double y = 2;  
	if (x * x == y) { cout << x; } else { cout << y; }
	return 0;
}
